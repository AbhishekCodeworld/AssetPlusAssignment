/*
//Not requried.
export const initialState ={
    posts : []
}

const reducer = (state, action ) => {
    switch(action.type) {
        case 'ADD_POST':
            return {
                posts : [...posts, ]
            }
        default:
            return state;
    }

};

export default reducer;

*/